{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn src.server:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/docs",  // atau "/" atau path lain yang tersedia
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
